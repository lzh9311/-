<% if logged_in? %>
    <div class="row">
        <aside class="col-md-4">
            <section class="user_info">
                <%= render 'shared/user_info' %>
            </section>
            <section class="micropost_form">
                <%= render 'shared/micropost_form' %>
            </section>
        </aside>
        <div class="col-md-8">
            <h3>已分享的数据集</h3>
            <%= render 'shared/feed' %>
        </div>
    </div>
<% else %>
    <div class = "center jumbotron">
        <h1>欢迎来到丑陋的微博主页</h1>
        <h2>这里啥也没有哈哈哈</h2>
        <%= link_to "开 始 探 索", signup_path, class: "btn btn-lg btn-primary" %></div>
        <img alt="Rails logo" src="assets/下载.jpg" />
    </div>
<% end %>

